# functions/utils/

Infrastructure helpers used by the simulator and live scripts.

| File | Purpose |
|---|---|
| `CancelToken.m` | Cancellation handle that bridges a worker's data queue to the simulator. |
| `updateLiveScriptProgress.m` | Live-script progress bar. |
